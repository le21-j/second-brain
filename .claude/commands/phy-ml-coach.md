---
description: Invoke the phy-ml-coach sub-agent — Physical-Layer ML Roadmap coach (NVIDIA Sionna + Wi-Lab targets)
argument-hint: <Python / ML / wireless-ML question>
---

Invoke the `phy-ml-coach` sub-agent (via the Agent tool with `subagent_type: "phy-ml-coach"`) and pass the request below as the prompt. The agent first re-checks the python-ml-wireless course page for the current Phase / milestone before answering.

Request: $ARGUMENTS
