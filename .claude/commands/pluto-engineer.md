---
description: Invoke the pluto-engineer sub-agent — 6G researcher persona for aircomp-regret-pluto work
argument-hint: <task / file / question>
---

Invoke the `pluto-engineer` sub-agent (via the Agent tool with `subagent_type: "pluto-engineer"`) and pass the request below as the prompt. The agent has the Pluto hardware constraints, FPGA/ARM split, and committed engineering decisions baked in.

Request: $ARGUMENTS
